import 'package:flutter/material.dart';

class GeneralAzkarTab extends StatelessWidget {
  const GeneralAzkarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('general'),
      ),
    );
  }
}
